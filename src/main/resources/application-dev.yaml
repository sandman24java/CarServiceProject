server:
  port: 8080
  servlet:
    context-path: /cars-app
app:
  env: dev
  show-history: true
  max-results: 100

logging:
  level:
    root: INFO
    com.example.module3: DEBUG
    org.springframework.web: DEBUG